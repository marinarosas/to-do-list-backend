export type TUserDB = {
    id: string,
    name: string,
    email: string,
    password: string
}

export type TTasksDB = {
    id: string,
    title: string,
    description: string,
    created_at: string,
    status: number
}